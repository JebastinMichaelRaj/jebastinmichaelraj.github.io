/* Public configuration only. Never place passwords or API keys here.
 * Leave blank for the included Node server, which injects its same-origin API.
 * For a separately hosted backend, enter its HTTPS origin, without a trailing slash.
 */
window.PORTFOLIO_CONFIG = Object.freeze({ apiBase: "" });
